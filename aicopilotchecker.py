import math
# create a function that returns the mean of a list of numbers
def mean(numbers):
    return sum(numbers) / len(numbers)

print(f'mean([1, 2, 3, 4, 5]) = {mean([1, 2, 3, 4, 5])}') # should print 3.0